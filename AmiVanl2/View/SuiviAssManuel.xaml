<UserControl x:Class="AmiVanl2.View.SuiviAssManuel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:oxy="http://oxyplot.org/wpf"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="1200">

    <Grid Background="White" Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Text="SUIVI ASS MANUEL"
            FontSize="30"
            FontFamily="Bahnschrift"
            FontWeight="Bold"
            HorizontalAlignment="Center"/>

        <TextBlock
            x:Name="TitreReference"
            Grid.Row="1"
            FontSize="18"
            FontFamily="Bahnschrift"
            FontWeight="SemiBold"
            Margin="0,15,0,15"
            HorizontalAlignment="Center"/>

        <ScrollViewer
            Grid.Row="2"
            HorizontalScrollBarVisibility="Auto"
            VerticalScrollBarVisibility="Disabled"
            Margin="0,0,0,10">

            <ItemsControl x:Name="ListeReferences">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel
                            Orientation="Horizontal"
                            HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button
                            Content="{Binding}"
                            Tag="{Binding}"
                            Margin="5"
                            Padding="14,7"
                            FontSize="13"
                            FontFamily="Bahnschrift"
                            FontWeight="SemiBold"
                            Cursor="Hand"
                            Click="BtnReference_Click"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

            </ItemsControl>
        </ScrollViewer>

        <StackPanel
            Grid.Row="3"
            Orientation="Horizontal"
            HorizontalAlignment="Center"
            Margin="0,0,0,15">

            <TextBlock
                Text="Opération :"
                FontSize="15"
                FontFamily="Bahnschrift"
                FontWeight="SemiBold"
                VerticalAlignment="Center"
                Margin="0,0,10,0"/>

            <ComboBox
                x:Name="ComboOperations"
                Width="180"
                Height="32"
                FontFamily="Bahnschrift"
                FontSize="14"
                SelectionChanged="ComboOperations_SelectionChanged"/>
        </StackPanel>

        <ScrollViewer
            Grid.Row="4"
            VerticalScrollBarVisibility="Auto"
            HorizontalScrollBarVisibility="Disabled">

            <ItemsControl x:Name="ListeCamemberts">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border
                            Width="420"
                            Height="350"
                            Margin="12"
                            BorderBrush="#DDD"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="15">

                            <oxy:PlotView Model="{Binding}" />

                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

            </ItemsControl>
        </ScrollViewer>

    </Grid>
</UserControl>