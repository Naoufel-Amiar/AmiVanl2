<UserControl x:Class="AmiVanl2.View.SuiviAssManuel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:oxy="http://oxyplot.org/wpf"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="1200">

    <UserControl.Resources>
        <Style x:Key="RefButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Bahnschrift"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="4,3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="#1E3A5F"
                                CornerRadius="20"
                                Padding="18,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#2563EB"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="White" Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock
            x:Name="TitrePage"
            Grid.Row="0"
            Text="SUIVI ASS MANUEL"
            FontSize="30"
            FontFamily="Bahnschrift"
            FontWeight="Bold"
            HorizontalAlignment="Center"
            Margin="0,0,0,15"/>

        <!-- Boutons de références -->
        <ScrollViewer
            Grid.Row="1"
            HorizontalScrollBarVisibility="Auto"
            VerticalScrollBarVisibility="Disabled"
            Margin="0,0,0,15">

            <ItemsControl x:Name="ListeBoutons">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button
                            Content="{Binding Reference}"
                            Tag="{Binding}"
                            Style="{StaticResource RefButtonStyle}"
                            Click="BtnReference_Click"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Titre référence sélectionnée -->
        <TextBlock
            x:Name="TitreReference"
            Grid.Row="2"
            FontSize="18"
            FontFamily="Bahnschrift"
            FontWeight="SemiBold"
            HorizontalAlignment="Center"
            Margin="0,0,0,5"/>

        <!-- Commentaire référence -->
        <Border
            x:Name="PanelCommentaire"
            Grid.Row="3"
            Background="#FFF8E1"
            BorderBrush="#F59E0B"
            BorderThickness="1"
            CornerRadius="6"
            Margin="20,0,20,8"
            Padding="12,8"
            Visibility="Collapsed">
            <StackPanel Orientation="Horizontal">
                <TextBlock
                    Text="💬 "
                    FontSize="14"
                    VerticalAlignment="Top"
                    Margin="0,1,4,0"/>
                <TextBlock
                    x:Name="TxtCommentaire"
                    FontSize="13"
                    FontFamily="Bahnschrift"
                    Foreground="#92400E"
                    TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- Infos résumé dynamiques -->
        <StackPanel
            x:Name="PanelInfos"
            Grid.Row="4"
            Orientation="Horizontal"
            HorizontalAlignment="Center"
            Margin="0,0,0,10"/>

        <!-- Graphiques dynamiques (N opérations) -->
        <ScrollViewer Grid.Row="5"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="PanelGraphiques">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="1"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="6,0"
                                BorderBrush="#DDD"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock
                                    Text="{Binding Titre}"
                                    FontSize="16"
                                    FontFamily="Bahnschrift"
                                    FontWeight="SemiBold"
                                    HorizontalAlignment="Center"
                                    Margin="0,0,0,5"/>
                                <oxy:PlotView
                                    Grid.Row="1"
                                    Model="{Binding Modele}"
                                    MinHeight="250"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

    </Grid>
</UserControl>
